# MOS.py 改进日志 - 最优解选择策略优化

## 修改时间
2026-07-26

## 问题描述

用户反馈每轮通讯得到的样本识别率得分不佳（0.3-0.4），需要改进最优解选择策略。

## 实施的改进

### 1. 详细得分日志输出 ✅

**位置**: 第723-751行（进化循环内）

**改动内容**:
- 每10代不仅打印平均识别率和破坏性
- 额外打印当前代最优个体（第一前沿首个）的详细信息
- 包含所有约束得分：Krum、Box、Sign、PCA、Subspace、Group

**新增日志格式**:
```
[MOS LOG]   Generation 10/100: 识别率=0.856, 破坏性=1234.567
[MOS LOG]     最优个体: 识别率=0.923, 破坏性=1456.789
[MOS LOG]     约束得分: Krum=0.95, Box=0.98, Sign=0.89, PCA=0.92, Subspace=0.87, Group=0.76
```

### 2. 理想点距离选择策略 ✅

**位置**: 第757-823行（进化完成后的最优解选择）

**核心改动**:

#### 2.1 打印Pareto前沿详细信息
```python
print(f"[MOS LOG] 📊 第一前沿个体详细得分：")
print(f"[MOS LOG] {'索引':<6} {'识别率':<8} {'破坏性':<12} {'Krum':<8} {'Box':<8} {'Sign':<8} {'PCA':<8} {'Subspace':<10} {'Group':<8}")
```
列出所有第一前沿个体的完整得分表格

#### 2.2 理想点定义
- **识别率**: 1.0（满分隐蔽性）
- **破坏性**: ∞（最大破坏力）

#### 2.3 硬性约束
```python
MIN_RECOGNITION_SCORE = 0.5  # 识别率得分必须≥0.5
```

#### 2.4 选择算法
1. 过滤Pareto前沿，只保留识别率≥0.5的个体
2. 如果无有效个体，降低阈值到0.3再试
3. 如果仍无有效个体，选择识别率最高的个体
4. 归一化破坏性到[0, 1]范围
5. 计算每个有效个体到理想点(1.0, 1.0)的欧氏距离
6. 选择距离最小的个体

**距离公式**:
```python
distance = sqrt((1.0 - recognition_score)^2 + (1.0 - normalized_destructiveness)^2)
```

#### 2.5 输出日志
```
[MOS LOG] 🎯 最优解选择策略：最接近理想点(识别率=1.0, 破坏性=∞)
[MOS LOG] 🎯 硬性约束：识别率得分 ≥ 0.5
[MOS LOG] 🏆 选中个体索引: 3
[MOS LOG]   ✓ 识别率得分: 0.823
[MOS LOG]   ✓ 破坏性: 1678.901
[MOS LOG]   ✓ 到理想点距离: 0.2156
```

## 技术细节

### 为什么不选首位个体
- **原因**: 非支配排序只保证Pareto最优性，不保证首位个体在识别率和破坏性上都最优
- **可能情况**: 首位个体可能是破坏性极高但识别率很低的解
- **问题**: 识别率0.3-0.4的解虽然在前沿上，但隐蔽性不足，容易被防御检测

### 理想点选择的数学原理
- **多目标优化**: 找到最接近"完美解"的可行解
- **归一化**: 将两个目标映射到同一尺度[0, 1]，避免数量级差异影响距离计算
- **欧氏距离**: 同时考虑两个目标的偏离程度，平衡识别率和破坏性

### 硬性约束的作用
- **保底隐蔽性**: 确保选中的解不会因为过度追求破坏性而牺牲隐蔽性
- **阈值设计**: 0.5表示至少通过一半的约束得分
- **降级策略**: 如果阈值过高导致无解，自动降低到0.3兜底

## 文件状态

- ✅ **文件大小**: 49,353 bytes
- ✅ **行数**: 935行
- ✅ **语法检查**: 通过 Python AST 解析

## 预期效果

1. **识别率提升**: 硬性约束0.5确保选中的个体隐蔽性达标
2. **破坏性保持**: 理想点距离选择在满足识别率的前提下最大化破坏性
3. **可观测性**: 详细日志让用户清楚看到每代的进化情况和最终选择依据

## 对比

| 项目 | 修改前 | 修改后 |
|-----|--------|--------|
| 选择策略 | 前沿首位 | 理想点距离最小 |
| 识别率约束 | 无 | ≥0.5（硬性） |
| 日志详细度 | 简略 | 完整约束得分 |
| 可解释性 | 低 | 高（距离+约束表） |

## 下一步

在服务器上运行测试，观察：
1. 最终选中的个体识别率是否≥0.5
2. 进化过程中约束得分的变化趋势
3. 理想点距离是否在合理范围（0.1-0.3）

---

**状态**: ✅ 修改完成，等待服务器测试验证
